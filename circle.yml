machine:
  timezone:
    Europe/Moscow    
 
test:
   post:
     - lein ring uberwar
    
deployment:
  pre:
     - cp $CIRCLE_PROJECT_REPONAME/target/*war $CIRCLE_ARTIFACTS
  staging:
    branch: master
    codedeploy:
      hello-circleci:
        deployment_group: CircleCI-Deployment
        