machine:
  timezone:
    Europe/Moscow
  python:
    version: 3.4.2
dependencies:
  pre:
    - sudo pip3 install --upgrade pip
    - sudo pip3 install boto
    - python3 $CIRCLE_PROJECT_REPONAME/aws-cfn/prepare.py
      