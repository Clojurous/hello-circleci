machine:
  timezone:
    Europe/Moscow    
 
test:
   post:
     - lein ring uberwar
     - cp $CIRCLE_PROJECT_REPONAME/target/*war $CIRCLE_ARTIFACTS
    
deployment:
  staging:
    branch: master
    codedeploy:
      hello-circleci:
        deployment_group: CircleCI-Deployment
        