machine:
  timezone:
    Europe/Moscow
  python:
    version: 3.4.2	
  pre:
    - sudo pip install --upgrade pip
    - sudo pip install boto
    - python3 $CIRCLE_PROJECT_REPONAME/aws-cfn/prepare.py
      